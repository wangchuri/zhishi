# 知拾本地配置
# 说明：本机自用单用户，无需鉴权。相对路径均以 backend/ 为基准。

# 本地存储（原始上传文件、解析产物、缩略图等）
storage_dir: ./storage

# 图床路径：markdown 中所有图片（images/*.png 等）都会落盘到这个位置
image_base_dir: ./storage/images

# 数据库
database_url: sqlite:///./data/zhishi.db

# 处理
# 最大并行处理数（上传解析 / 分段 / 出题等并发任务上限），默认 1
max_concurrency: 1
